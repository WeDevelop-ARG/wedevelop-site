{
  "name": "wedevelop-site-firestore",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "firebase deploy --project=$npm_config_project --only=firestore:rules"
  },
  "devDependencies": {
    "firebase-tools": "^11.22.0"
  }
}
