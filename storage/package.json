{
  "name": "wedevelop-site-firestore",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "firebase deploy --project=$npm_config_project --only=storage:rules"
  },
  "devDependencies": {
    "firebase-tools": "^9.5.0"
  }
}
